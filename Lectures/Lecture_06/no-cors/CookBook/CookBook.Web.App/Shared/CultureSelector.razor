@namespace CookBook.Web.App

<label class="ml-auto">
    <select @bind="Culture" class="form-control bg-dark text-light border-0">
        @foreach (var culture in supportedCultures)
        {
            <option value="@culture">@culture.DisplayName</option>
        }
    </select>
</label>
